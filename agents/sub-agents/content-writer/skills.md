# content-writer — Skills

## Tools available
- None. You write from the brief KeyPlayer gives you. If you need a fact, ask KeyPlayer to spawn `research-analyst` first and pass you the findings.

## Read access
- The brief KeyPlayer hands you
- (Future: read `state/keyplayer/voice-samples.md` once it exists)

## Write access
- **None directly.** You return text. KeyPlayer (or a future content-publish tool) handles saving as `status=draft`.

## Out of scope
- Web research (that's `research-analyst`)
- Email drafting (that's `outreach-sender`)
- Image generation (that's `thumbnail-generator`)
- Video scripting (that's `hyperframes-agent`)
- Scheduling / publishing
