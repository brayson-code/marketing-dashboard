# Supabase — keyplayers-command-center (project ref: zgtiviorskkcuzxnpvha)
# Copy the values you need into .env.local (and later into Vercel env vars).

# ── Public (safe to expose to the browser / frontend) ────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://zgtiviorskkcuzxnpvha.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_JdEtglorlXwt1bwLN3NkJg_-eFXnvqV

# ── Backend ONLY — SECRET. Never commit. Never send to the browser. ──────────
# Supabase Dashboard -> Settings -> Database -> Connection string -> "Transaction pooler" (port 6543).
# Paste the EXACT URI shown there (it includes your DB password). Example shape:
# postgresql://postgres.zgtiviorskkcuzxnpvha:YOUR-DB-PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres
SUPABASE_DB_URL=

# Optional — only if a backend feature uses supabase-js (auth admin / storage):
# Supabase Dashboard -> Settings -> API -> service_role key
# SUPABASE_SERVICE_ROLE_KEY=
